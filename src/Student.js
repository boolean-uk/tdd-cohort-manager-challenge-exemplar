
class Student {
  constructor(id, firstName, lastName, githubAccount, email) {
    this.studentId = id
    this.firstName = firstName
    this.lastName = lastName
    this.githubAccount = githubAccount
    this.email = email
  }
}

module.exports = Student